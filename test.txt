class Yay
{
	static int x;
	static int blyat() {
		ifj16.print("sdf");
		while(df) {
			Meh.asdon(asd);
		
		}
		Yay.x = 5;
	}
}

// Komentik vyjebany
/*
class Main
{
	static int x;
	static void run()
	{
		String str1;
		str1 = "Toto je nejaky text";
		String str2;
		str2 = str1 + ", ktery jeste trochu obohatime";
		x = find(str2, "text");
		print("Pozice retezce \"text\" v retezci str2: " + x + "\n");
		play(str1);
	} // end of static void run()
} // end of class Main
class Game
{
	static void play(String str)
	{
		print("Zadejte nejakou posloupnost vsech malych pismen a-h, ");
		print("pricemz se pismena nesmeji v posloupnosti opakovat:");
		str = readString();
		str = sort(str);
		int cmp = compare(str, "abcdefgh");
			if (cmp != 0) {
				while (cmp != 0) {
				print("Spatne zadana posloupnost, zkuste znovu:");
				str = readString();
				str = sort(str);
				cmp = compare(str, "abcdefgh");
				}
			}
			else {
			}
		return;
	} // end of static v
}
*/